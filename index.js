
const checkAmap = require('./src/checkAmap')()

const pixel = require('./src/pixel')
const size = require('./src/size')
const marker = require('./src/marker')

module.exports = {
  pixel,
  size,
  marker
}
